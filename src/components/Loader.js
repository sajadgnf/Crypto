import React from 'react';

// gif
import Spinner from "../gif/spinner.gif"

const Loader = () => {
    return (
        <div>
            <img src={Spinner} />
        </div>
    );
};

export default Loader;